{
  "routes": [
    {
      "method": "GET",
      "path": "/post",
      "handler": "Post.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/post/:id",
      "handler": "Post.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/post",
      "handler": "Post.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/post/:id",
      "handler": "Post.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/post/:id",
      "handler": "Post.destroy",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/post/:id/relationships/:relation",
      "handler": "Post.createRelation",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/post/:id/relationships/:relation",
      "handler": "Post.updateRelation",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/post/:id/relationships/:relation",
      "handler": "Post.destroyRelation",
      "config": {
        "policies": []
      }
    }
  ]
}
